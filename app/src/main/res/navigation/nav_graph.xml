<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/home">

    <fragment
        android:id="@+id/home"
        android:label="fragment_item_home"
        tools:layout="@layout/fragment_item_home" />
    <fragment
        android:id="@+id/add"
        android:label="fragment_item_add"
        tools:layout="@layout/fragment_item_add" />
    <fragment
        android:id="@+id/profile"
        android:label="fragment_item_profile"
        tools:layout="@layout/fragment_item_profile" />
</navigation>